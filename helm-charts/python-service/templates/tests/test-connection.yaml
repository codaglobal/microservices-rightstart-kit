apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python-service.fullname" . }}-test-connection"
  labels:
{{ include "python-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "python-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
